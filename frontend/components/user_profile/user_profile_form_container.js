import { connect } from 'react-redux';
import UserProfileForm from './user_profile_form';
import {  } from '../../actions/users_actions';

const mapStateToProps = (state) => {
  return {
    
  }
}

const mapDispatchToProps = (dispatch) => {
  return {

  }
}

export default connect(
  mapStateToProps,
  mapDispatchToProps
)(UserProfileForm);